#include <stdio.h>
#include <string.h>
int main() {
    printf("nhap chuoi ");
    char s[100], c;
    int n;
    fgets(s, 100, stdin);
    s[strlen(s)-1] = 0;
    get(c);
    scanf("%d", &n);
    while (i<strlen(s)) {
        if (s[i])
    }
    
}